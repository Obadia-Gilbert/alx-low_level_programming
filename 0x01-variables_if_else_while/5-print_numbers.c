#include <stdio.h>
/**
 * main - Entry Point
 * Description: print number from 1 to 10
 * Return: Always 0 (success)
 */
int main(void)
{
	int n;

	for (n = 0; n > 10; n++)
	{
		printf(n);
	}
	printf('\n');
	return (0);
}
